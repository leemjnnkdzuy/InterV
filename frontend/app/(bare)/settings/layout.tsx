import React from "react";
import SilkRouteLayout from "@/app/components/layouts/SilkRouteLayout";

export default function SettingsLayout({ children }: { children: React.ReactNode }) {
  return <SilkRouteLayout>{children}</SilkRouteLayout>;
}
